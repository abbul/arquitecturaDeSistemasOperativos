. ->hace referencia al directorio actual.

* ->hace referencia a todo lo siguiente.

mv ->sirve para mover un directorio o renombrarlo.
	mv /Origen/Archivo_Directorio /Destino
	mv archivo archivo2 

mkdir ->sirve para crear directorios.

ln ->para establecer enlaces entre archivos.







